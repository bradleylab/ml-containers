# Pinned for reproducibility. PyTorch is provided by the base image.
huggingface_hub>=0.26
hydra-core>=1.3.2
iopath>=0.1.10
numpy>=1.26
pillow>=10.0
pycocotools>=2.0.8
tqdm>=4.66
omegaconf>=2.3
# Geospatial wrappers around SAM 2 — tiled inference on large GeoTIFFs,
# georeferenced mask output, polygonisation to GeoPackage / GeoJSON.
# Pulls geopandas + rasterio + shapely + pyproj transitively. Adds
# ~500 MB to the image but means a single container can take a GeoTIFF
# in and emit georeferenced polygons out, without project-side tiling
# boilerplate. Imported as `samgeo.samgeo2.SamGeo2` for SAM-2 specifically.
segment-geospatial>=0.10
geopandas>=0.14
rasterio>=1.3
shapely>=2.0
pyproj>=3.6
